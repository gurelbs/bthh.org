// import * as mongoose from 'mongoose';

// const { db_connect, db_str } = process.env;

// export const databaseProviders = [
//   {
//     provide: db_connect,
//     useFactory: (): Promise<typeof mongoose> => mongoose.connect(db_str),
//   },
// ];
